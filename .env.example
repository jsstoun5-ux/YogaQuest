# YogaQuest Environment Variables
# Copy this file to .env and fill in the values

# API URL for server-side storage (used when Telegram CloudStorage is unavailable or exceeds limits)
# Leave empty to use relative paths (same domain)
# Example: https://api.yogaquest.app
VITE_API_URL=
